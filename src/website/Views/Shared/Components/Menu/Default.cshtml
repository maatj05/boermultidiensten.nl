@model mwg.cms4.core.interfaces.entities.IMenuItem




@*<nav>

<div class="navbar navbar-default navbar-fixed-top">
    <div class="container">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a asp-area="" asp-controller="Home" asp-action="Index" class="navbar-brand">Boer Multidiensten</a>
        </div>
        <div class="navbar-collapse collapse" id="nav1">
            <ul class="nav navbar-nav">

                @foreach (var item in Model.SubMenus)
            {
                if (item.SubMenus.Any())
                {
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">@item.MenuText <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                @foreach (var subitem in item.SubMenus)
                                {

                                    if (subitem.FysicalUrl == "-")
                                    {
                                        //seperatur
                                        <li role="separator" class="divider"></li>
                                        <li class="dropdown-header">@subitem.MenuText</li>
                                    }
                                    else
                                    {

                                    <li><a href="@subitem.FysicalUrl">@subitem.MenuText</a></li>

                                    }


                                 



                                }
                            </ul>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a href="@item.FysicalUrl">@item.MenuText</a>
                        </li>
                    }
                }



            </ul>
          
        </div>
    </div>
</div>
</nav>*@


<nav>
    <ul>
        @foreach (var item in Model.SubMenus)
        {
            <li><a href="@item.FysicalUrl">@item.MenuText</a></li>
        }
    </ul>
</nav>